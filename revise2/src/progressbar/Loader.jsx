import Progress from "./Progress"

const Loader = () => {
  return (
    <div>
        <Progress width = {75} />
        <Progress width = {100} />
        <Progress width = {60} />
    </div>
  )
}

export default Loader